# This is a Databricks asset bundle definition for databricks_project.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: databricks_project

variables:
  uploaded_data_path:
    description: "Path to the uploaded data directory"
    default: "/Volumes/nyctaxi/00_landing/data_sources/lookup/"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405610591599065.5.azuredatabricks.net
    variables:
      uploaded_data_path: "/Volumes/nyctaxi/00_landing/data_sources/lookup/"
  

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-7405610591599065.5.azuredatabricks.net
